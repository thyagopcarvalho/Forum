<h2>Tópicos</h2>
<% @posts.each do |post| %>
<div class="divider"></div>
	<div class="section">
		<h3><%= link_to post.title, post%></h3>
			<p>	Publicado a <%=time_ago_in_words(post.created_at)%>
				by <%= post.user.email%>
			</p>
		<%end%>
	</div>
